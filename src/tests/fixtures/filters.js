import moment from 'moment'

const filters = {
  test: "",
  sortBy: "date",
  startDate: undefined,
  endDate: undefined
}

const altFilters = {
  test: "e",
  sortBy: "amount",
  startDate: undefined,
  endDate: undefined
}